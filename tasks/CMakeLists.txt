# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(adap_samples_inputTaskLib)
ADD_LIBRARY(${ADAP_SAMPLES_INPUT_TASKLIB_NAME} SHARED 
    ${ADAP_SAMPLES_INPUT_TASKLIB_SOURCES})
add_dependencies(${ADAP_SAMPLES_INPUT_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${ADAP_SAMPLES_INPUT_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${ADAP_SAMPLES_INPUT_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${ADAP_SAMPLES_INPUT_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${ADAP_SAMPLES_INPUT_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${ADAP_SAMPLES_INPUT_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${ADAP_SAMPLES_INPUT_TASKLIB_HEADERS}
    DESTINATION include/orocos/adap_samples_input)

